/** @type {import('next').NextConfig} */
const nextConfig = {
    // Enable Server Actions if needed, though we primarily use Route Handlers here
    experimental: {
        // serverActions: true,
    },
};

export default nextConfig;
